{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "request.schema.json",
  "title": "RegistrationRequest",
  "description": "Registrierungsanfrage eines neuen Benutzers (ausstehende Genehmigung). Datei: requests/{kuerzel}-request.json",
  "type": "object",
  "required": ["kuerzel", "name", "vorname", "businessMail", "requestedAt"],
  "properties": {
    "kuerzel": {
      "type": "string",
      "pattern": "^[a-z0-9]+$",
      "maxLength": 30,
      "description": "Gewünschtes Login-Kürzel (nur Kleinbuchstaben und Ziffern)"
    },
    "name": {
      "type": "string",
      "description": "Nachname des Antragstellers"
    },
    "vorname": {
      "type": "string",
      "description": "Vorname des Antragstellers"
    },
    "businessMail": {
      "type": "string",
      "format": "email",
      "description": "Dienstliche E-Mail-Adresse des Antragstellers"
    },
    "chapterId": {
      "type": "string",
      "description": "Optional gewünschtes Chapter für den Antrag"
    },
    "sparte": {
      "type": "string",
      "description": "Optional gewünschte Sparte innerhalb des gewünschten Chapters"
    },
    "bemerkung": {
      "type": "string",
      "description": "Optionale Nachricht / Bemerkung des Antragstellers"
    },
    "requestedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Zeitpunkt des Eingangs der Anfrage (ISO 8601)"
    }
  },
  "additionalProperties": false
}
